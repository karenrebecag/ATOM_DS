/**
 * Badge re-export from flat atoms directory
 * Preserves backward compatibility while enabling subdirectory organization.
 */
export { Badge } from '../Badge';
export type { BadgeProps } from '../Badge';
